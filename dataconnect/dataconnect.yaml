specVersion: "v1"
serviceId: "kg-note"
location: "us-west1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "kg-note-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
